/**
 * Gère l'affichage et les interactions de la page des favoris
 */
const currentPage = 'favoris';

if (document.body.classList.contains(`${currentPage}`)) {
    console.log(`Is Page => '${currentPage}'`);
}